import socketMiddleware from '../../../server/web/sockets/middleware'

describe('SocketCluster socket middleware', () => {
  it('should auth subscriptions')
  
  it('should kick on disconnect')
  
  it('should whitelist client2client actions')
})